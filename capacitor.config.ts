import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vanfreyr.dndcharactersheet',
  appName: 'D&D Character Sheet',
  webDir: 'dist'
};

export default config;
